[dataget.go](dataget.go) `data.get` executor that reads JSON/YAML files, resolves jq queries, and returns queried values with default fallback handling.
[dataget_test.go](dataget_test.go) Table-driven tests for `data.get` covering file parsing, query evaluation, defaults, and failure cases.
