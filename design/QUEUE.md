# Design Doc Queue

Always re-open this file right before editing; never rely on a cached copy.

## Legend
- [ ] `docs/design/<subject>/README.md` — design doc is planned and unclaimed.
- [ ] `docs/design/<subject>/README.md` (in progress) — design doc is currently being implemented; only one agent should hold this state.
- Remove the entry once the design doc is verified and closed so downstream work can advance.

## Queue
- *(Add design docs here in dependency order: ready-to-pull work at the top, most constrained items last.)*
